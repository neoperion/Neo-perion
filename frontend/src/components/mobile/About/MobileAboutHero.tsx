// Re-uses the shared cinematic AboutHero (video background, clip-reveal headline, stats)
// The component is already responsive — mobile gets the same cinematic treatment.
export { AboutHero as MobileAboutHero } from '@/components/about/AboutHero';
